[project]
name = "mock-mes-kr"
version = "0.1.0"
description = "Throwaway mock semiconductor-fab MES for agent demos: shared SQLite via web console, REST API, and MCP server."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<0.200",
    "uvicorn[standard]>=0.30,<0.40",
    "jinja2>=3.1,<4",
    "python-multipart>=0.0.9",
    "mcp>=1.9,<2",
    "starlette>=0.37",
    "httpx>=0.27",
]

[tool.setuptools]
packages = ["mes_core", "api", "mcp_server"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
